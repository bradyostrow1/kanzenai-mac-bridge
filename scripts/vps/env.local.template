# /opt/kanzen/.env.local — VPS environment template
#
# This file holds the ROTATED secrets after Brady rotates the 10 keys
# exposed earlier. Paste real values into the slots below — do NOT
# commit this file with values, and do NOT copy the old (compromised)
# keys into here.
#
# Permissions on the VPS must be tight:
#   sudo install -m 600 -o brady -g brady env.local.template /opt/kanzen/.env.local
#   then edit /opt/kanzen/.env.local in place.

# ─── Claude (rotate before paste) ─────────────────────────────────
ANTHROPIC_API_KEY=

# ─── Resend (rotate before paste) ─────────────────────────────────
RESEND_API_KEY=
RESEND_AUDIENCE_ID=bda2ca0a-9b2d-4536-8a59-bd38456e623d
RESEND_FROM=hello@kanzenai.com

# ─── Pexels (rotate before paste) ─────────────────────────────────
PEXELS_API_KEY=

# ─── X / Twitter (rotate ALL 7 before paste — these can post as you) ─
X_CONSUMER_KEY=
X_CONSUMER_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
X_BEARER_TOKEN=
X_CLIENT_ID=
X_CLIENT_SECRET=

# ─── Optional: affiliate-network polling for vendor-approval-tracker ─
# Leave blank — the tracker skips any network without credentials.
# Add these once Brady actually applies + gets approved on each network.
# AWIN_API_KEY=
# AWIN_PUBLISHER_ID=
# SHAREASALE_API_TOKEN=
# SHAREASALE_API_SECRET=
# SHAREASALE_AFFILIATE_ID=
# IMPACT_ACCOUNT_SID=
# IMPACT_AUTH_TOKEN=
# CJ_PERSONAL_ACCESS_TOKEN=
# REFERSION_API_KEY=

# ─── Optional: Telegram alerts (not currently used by Kanzen) ─────
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
