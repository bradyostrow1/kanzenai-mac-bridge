# dependencies
node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage

# next.js
.next/
out/

# production
build
dist

# misc
.DS_Store
*.pem
.vercel

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (local secrets — never commit)
.env*.local
.env

# affiliate-link backups
content/**/*.json.bak
.audit/

# x-strategy runtime state (per-host — Bot 11 writes these on the VPS).
# Only the default config is checked in; the live one is per-host.
config/x-strategy.json
config/x-strategy-changelog.jsonl

# real affiliate codes — never commit
affiliate-codes.json

# typescript
*.tsbuildinfo
next-env.d.ts
.env*
